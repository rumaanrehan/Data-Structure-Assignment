#include<bits/stdc++.h>
using namespace std;
/*
void display(int[], int);
void reverseBlock(int[], int);

void display(int a[], int n)
{
    cout<<"\n Elements of array : \n";
    for (int i=0; i<n; i++)
    {
        cout<<a[i]<<"\t";
    } 
}

void reverseBlock(int a[], int n)
{
    int k,i;
    cout<<"\n enter blocks of nodes to be reversed\t";
    cin>>k;
    for (i=0; i<n; i=i+k)
    {
        for(int j=i;j<(i+k+1)/2;j++)
        {
            int t=a[j];
            a[j]=a[i+k-j-1];
            a[i+k-j-1]=t;
        }
    }
    display(a,n);
}

int main()
{
    int a[20], n, i;
    cout<<"\n Enter size of array :\t";
    cin>>n;
    cout<<"\n Enter elements in array \n";
    for (i=0; i<n; i++)
    {
        cin>>a[i];
    }
    display(a,n);
    reverseBlock(a,n);
    return 0;

}
*/


